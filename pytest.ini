[pytest]
testpaths = tests
# Ensures pytest uses this directory as root regardless of where it's invoked from
markers =
    integration: integration tests (slower, cross-component)
    e2e: live end-to-end tests (spawn real `opencode` subprocess + model calls; run via ./run-tests.sh e2e)
