{
  "name": "claude-recall-plugin-ts-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsc && node -e \"require('fs').writeFileSync('out/package.json', JSON.stringify({type:'module'}))\" && node --test $(find out -name '*.test.js')"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
